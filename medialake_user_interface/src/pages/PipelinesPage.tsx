import React from 'react';
import { Box, Typography } from '@mui/material';

const PipelinesPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Pipelines
            </Typography>
            {/* Pipelines content will go here */}
        </Box>
    );
};

export default PipelinesPage;
