{
    "status": "{{ response.status }}",
    "progress": {{ response.progress | default(0) }},
    "message": "{{ response.message | default('') }}",
    "completed_at": "{{ response.completed_at | default('') }}"
}
