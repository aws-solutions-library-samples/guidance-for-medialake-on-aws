spec: v1.0.0
node:
  id: trigger_workflow_completed
  title: Workflow Completed
  description: Trigger after a workflow is completed
  version: 1.0.0
  type: trigger
  integration:
    config:
      aws_event_bridge:
        aws_eventbridge_rule: vide_trigger
actions:
  trigger:
    summary: Trigger after a workflow is completed
    description: This step will trigger after a workflow is completed
    operationId: triggerWorkflowCompleted
    parameters:
      - in: body
        name: Workflow Name
        required: true
        schema:
          type: string
    connections:
      incoming:
        type: [any]
      outgoing: 
        type: [any]