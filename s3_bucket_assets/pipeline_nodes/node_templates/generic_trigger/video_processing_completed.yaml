spec: v1.0.0
node:
  id: trigger_video_processing_completed
  title: Video Processing Completed
  description: Wait for a specific duration
  version: 1.0.0
  type: trigger
  integration:
    config:
      aws_eventbridge:
        aws_eventbridge_rule: video_processing_completed
actions:
  trigger:
    summary: Trigger after video processing is completed
    description: This step will trigger after video processing is completed
    operationId: triggerVideoProcessingCompleted
    parameters:
      - in: body
        name: Video Type
        required: true
        schema:
          type: string
    connections:
      incoming:
        type: [any]
      outgoing: 
        type: [any]